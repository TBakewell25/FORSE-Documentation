Extending the Model
===================

.. This page is for developers who want to modify or build on FORSE —
   adding species, disturbances, growth factors, or improving performance.


Adding a New Species
--------------------

.. Walk through the steps required to add a species:
   1. Add the allometric height equation to allomlib.py (describe the function signature)
   2. Add per-species parameters to the driver file (reference driver_reference.rst)
   3. Increment NSPECIES in the driver
   4. Any other places in the code that need updating
   Mention any constraints (e.g. species must be native to the simulated region,
   parameters must come from field data or literature).


Adding a New Disturbance
------------------------

.. Describe the disturbance plugin pattern used by logging_treatment.py:
   - The function signature expected by forse.py
   - What matrices are passed in and what the function should modify
     (set DBH = 0 and species = -1 for removed trees)
   - How to register the new disturbance type in the driver file
   Walk through logging_treatment.py's stripcut() as a concrete example.


Adding a New Growth Factor
--------------------------

.. Describe how to write a new environmental growth factor:
   - A factor function takes weather/soil/driver data and returns a matrix
     of shape (nx, ny, nspecies) with values between 0 and 1
   - 0 = complete growth limitation, 1 = no limitation
   - Where in the annual loop to call it (after weather generation, before grow_trees)
   - How to multiply it into the existing factor product in grow_trees()
   Mention that factors should be validated against field observations.


Performance Notes
-----------------

Numba JIT Compilation
^^^^^^^^^^^^^^^^^^^^^

.. Explain when and why Numba is used in FORSE:
   - The 3D light calculation in light3d.py is the primary use case
   - @numba.jit(nopython=True) requirements (no Python objects, typed arrays)
   - The --no-numba flag and when it is useful (debugging, unsupported platforms)
   - First-run compilation overhead (subsequent runs are faster)

Specialized Driver Files
^^^^^^^^^^^^^^^^^^^^^^^^

.. Explain what the numba_specialized_driver_* files are:
   - Auto-generated by create_specialized_driver_code.py
   - Pre-compile species-specific functions for the Numba JIT
   - When and why to regenerate them

Profiling
^^^^^^^^^

.. Mention profile_raytracer.py and how to use it to identify bottlenecks.
